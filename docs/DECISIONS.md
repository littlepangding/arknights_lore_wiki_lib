# Decision Log

> Tracks substantial user instructions and project direction changes.
> Each entry summarizes the user's intent so future sessions have full context.

### 2026-05-08 — Build an agent-readable knowledge base

**User intent:** Stand up a parsed, searchable KB on top of `ArknightsGameData` so an LLM agent can do ad-hoc lore Q/A, audit new wiki updates, and audit existing wiki pages — all from `arknights_lore_wiki_lib/` as the working root. Code-and-tests should do the deterministic work; LLM access should be offloadable to Gemini CLI / Gemini API / Claude CLI rather than burning the agent's own context. Prompts should be written in Chinese to match the source. Document everything in `docs/` so reviewers (or future agents) can audit the design before any implementation.

**Outcome:** Created `docs/REQUIREMENTS.md` (verbatim user requirements) and `docs/DESIGN.md` (architecture for `libs/kb/`, `data/kb/` raw chunks gitignored, `kb_summaries/` LLM-derived navigation aids in git, retrieval via structured indexes + grep, no embeddings in v1, three-backend LLM dispatch). Implementation deferred pending review.

### 2026-05-08 — KB layout decisions (clarified before design)

**User intent:** Settle four load-bearing layout questions before drafting the design doc, so the design doesn't have to fork on speculation.

**Outcome:** Decisions:
- KB on disk: `arknights_lore_wiki_lib/data/kb/` (gitignored raw + index), `arknights_lore_wiki_lib/kb_summaries/` (in git: LLM-made summaries).
- KB scope: **raw game data only**. Existing wiki summaries (`arknights_lore_wiki/data/`) are an audit target, not a query layer.
- LLM backends: all three (Gemini CLI default, Gemini API, Claude CLI) — user explicitly added "gemini cli as default".
- Retrieval: structured indexes + grep, no embeddings in v1.

### 2026-05-08 — Design revisions after Codex reviews 01 & 02

**User intent:** Run two reviewer passes on the design before implementation. Both reviews landed at `docs/reviews/2026-05-08-codex-review-{01,02-kb-structure}.md`. The user said: *"update your design if you think the critics makes sense"*. Findings were evaluated and applied.

**Outcome:** The following structural changes to `docs/DESIGN.md` (and matching updates to `docs/AGENTS_GUIDE.md`, `docs/REQUIREMENTS.md`):
- **Source families as first-class navigation axis** (replaces `entryType` browse). Four families derived deterministically: `mainline`, `activity`, `mini_activity`, `operator_record` (was `entryType=NONE`, 372/461 events), plus residual `other`. `events_by_family.json` replaces `events_by_type.json`. CLI `--type` becomes `--family`. Empirically verified against current corpus.
- **Sectional character data** replaces the monolithic `<char_id>.txt` blob. Each char now lives at `chars/<char_id>/{profile,voice,archive,skins,modules}.txt` plus `manifest.json` and `storysets.json`. CLI gains `char get <id> --section <name>`.
- **Deterministic char↔event edges** are first-class. Built from handbook storysets via `storyTxt` lookup — empirically 372 / 372 unique linkage with zero ambiguity. Stored in `char_to_events_deterministic.json`. Grep-based edges remain as `char_to_events_inferred.json` (recall floor, never duplicates deterministic edges). Every CLI cross-reference command takes `--source deterministic|inferred|both`.
- **`source_family` and `storyTxt_prefix` are first-class metadata** on every event and stage row.
- **`char_alias.json` is now OPTIONAL enrichment**, not a build prerequisite. KB builds entirely from `ArknightsGameData` if the wiki repo's alias file is absent. (Resolves R01 Finding 3.)
- **Added `## Measurements` section to DESIGN.md** with reproducible commands for every size/count claim — supersedes inline assertions. (Resolves R01 Finding 4.)
- **Open question 6** in DESIGN.md surfaces the pre-existing `.agents/skills/audit-lore-wiki/SKILL.md` issue (R01 Finding 1) — that skill references nonexistent scripts; not introduced by this design but flagged for the user to decide. (Surfaces R01 Finding 1.)
- Root-level `CLAUDE.md` / `AGENTS.md` will get a small KB pointer block. (Resolves R01 Finding 2 — pending separate edit.)

### 2026-05-08 — Design revisions after Codex review 03 (alias readiness)

**User intent:** Third reviewer pass landed at `docs/reviews/2026-05-08-codex-review-03-updated-doc-alias.md`, focused on whether the revised KB design can actually handle aliases from raw game data alone. User asked for the same evaluate-and-apply treatment.

**Outcome:** Empirically verified review's claims (9 duplicate display names; 33/348 curated aliases match `name`; 3/348 match `appellation`; 93/265 curated canonicals not in `character_table` at all). Updated docs to be honest about scope:
- **`Entity model and v1 scope` section added to DESIGN.md.** v1 is operator-centric. NPCs / titles / groups (e.g. `特蕾西娅`, `整合运动`) are addressable only via `grep_text`, not via name resolution. v2 deferred — would add an `entities/` parallel layer keyed by `extended_<slug>` (matching existing `get_char_file_name` convention).
- **`Aliases: where they come from` section added.** Three-tier alias source table (name / appellation / curated file) with measured coverage. `manifest.aliases` content explicitly differs in raw-only vs enriched modes.
- **`extract_data_from_character_table` planned extension to keep `appellation`** (currently dropped). Adds English codename support (Lancet-2, Amiya, etc.). Documented as narrow-scope, not general alias coverage.
- **`resolve_char` renamed to `resolve_operator_name` with tagged-union return** (`Resolved | Ambiguous | Missing`). Surfaces ambiguity for the 9 duplicate names rather than silently picking. Honest naming about operator-only scope.
- **AGENTS_GUIDE.md gains a "what the resolver does and doesn't cover" subsection** with worked Missing → grep fallback example.
- Risks table updated with three new rows (lore-entity coverage gap, curated alias coverage, ambiguity behavior).

### 2026-05-08 — Demote .agents/skills/audit-lore-wiki/ from live skill to design proposal

**User intent:** Asked whether `.agents/skills/audit-lore-wiki/SKILL.md` is a previous incremental update process worth keeping or an orphan. Answer: orphan — `git log --all -- 'scripts/audit*'` returns 0 commits, the skill is `??` (untracked) in `git status`, and the TSVs in `tmp/` are stale 2025-vintage hand artifacts whose mtimes were touched today. The 5-pass framework (tags / coverage / xref / grounding / sticker_loss) is substantive design content though, not noise — directly informs Phase 6 `kb_audit_wiki.py`.

**Outcome:** User said "yes" to demoting. Move file to `docs/proposals/audit-lore-wiki-prior-spec.md`. Future `kb_audit_wiki.py` should reference / adapt this prior spec — the grounding pass especially is what the new audit subsumes.

### 2026-05-08 — Design revisions after Codex review 04 (simplicity / cost / correctness)

**User intent:** Fourth reviewer pass landed at `docs/reviews/2026-05-08-codex-review-04-simplicity-cost-correctness.md`. User asked for the same evaluate-and-apply treatment.

**Outcome:** All four findings empirically reproduced, then applied:
- **M4 measurement command fixed and recorded result corrected.** Earlier draft crashed because `get_char_info_text_prompt` requires `name`; recorded count `~2900` was a transcription error. Verified result: 449 entries in `get_all_char_info`, 444 with name, char-text size median=5484 / p95=9016 / max=11460. Fixed snippet adds the `if v.get('name')` guard.
- **Added M5 measurement** for event-size + stage-count distribution. Verified: 78 events > 50K, 51 > 80K, 35 > 100K, 11 > 150K, 2 > 200K; stages per event median 1, p90 19, max 41, 69 events have >10 stages.
- **Multi-pass summary trigger lowered** from `total_length > 200K` to `total_length > 80K OR stage_count > 10`. Affects ~70-90 events instead of just 2 — actually cost-aware now.
- **Per-character LLM summaries dropped from v1.** Removed `kb_summaries/chars/`, `kb_summarize.py --target chars`, `kb_query summary char`. Char data is already sectional and small; a one-line summary would duplicate `manifest.json`. P2 prompt in `PROMPTS.md` marked DEFERRED for possible v2.
- **Stage-precise deterministic edges restored** in `event_to_chars.json` (was lossy in earlier draft). Added `stage_chars(event_id, stage_idx)` query and corresponding `kb_query event stage_chars` CLI. Updated AGENTS_GUIDE workflow recipe to use `stage_chars` for tight scope rather than overstating what `event chars` guarantees.
- `event_chars` and `stage_chars` now return `Appearance` (with `stage_idx` + `source`) rather than `CharMeta`, matching the data shape and preserving relation metadata.

### 2026-05-08 — Design revisions after Codex reviews 05 (follow-up) and 06 (independent)

**User intent:** Two more reviewer passes landed at `docs/reviews/2026-05-08-codex-review-05-followup.md` (4 findings) and `docs/reviews/2026-05-08-codex-review-06-independent.md` (3 findings, P1/P1/P2 priority). User asked for joint evaluate-and-apply across both, since 06 is independent of the prior conversation.

**Outcome:** Empirically verified each finding (5 nameless `npc_*` records, 23 single-zh-char operators incl. `陈/年/夕/黑/令`, `暮落` collides on `char_512_aprot` + `char_4025_aprot2`, `暮落;沉渊` is line 93 of `char_alias.txt`). All 7 findings applied:

- **Build rule tightened from "any data" to "has `name`"** (R05 #1). 5 nameless `npc_*` records explicitly skipped + listed in build report. Manifest schema requires `name`, so this prevents emitting unusable operator manifests.
- **PROMPTS.md multi-pass threshold synced to DESIGN.md** (R05 #2). Was still saying `> 200 KB`; fixed to `total_length > 80,000 OR stage_count > 10`. Added a callout linking back to M5.
- **`grep_text` made literal-by-default; `--regex` opt-in** (R05 #3). The fallback path is hit hardest by NPC/group names with parens / hyphens / smart quotes, where regex-by-default is brittle. `kb_query grep "<text>" [--regex]` mirrors the API change.
- **Inferred-edge matcher split into three classes** (R06 #1). Class A (canonical names + appellation) has **no length floor** — restores recall on 23 single-char operators. Class B (curated aliases) and C (fuzzy) keep the 2-char floor. Each edge records its `match_class` so consumers can downweight `canonical_short` hits without forced filtering.
- **Audit redesigned to two-signal model** (R05 #4 + R06 #2). Signal 1: entity-coverage diff (catches omissions). Signal 2: claim-level LLM coverage check with `有依据 / 无依据 / 不确定` verdicts (catches wrong attributions + hallucinations between known entities — what entity-diff alone misses). Both run by default. Budget caps baked into `kb_audit_wiki.py` as constants: ≤30 claims/event, ≤15 omission candidates/event, 3 stages × 8K chars per call, ~150K input-token soft budget. `--audit-all` opts out for long mainline events.
- **Curated-alias join now refuses ambiguous canonicals** (R06 #3). When `canonical` collides with a duplicate display name (`暮落` etc.), the alias line goes into a separate `ambiguous_aliases` map keyed by canonical, NOT into either operator's `manifest.aliases`. `resolve_operator_name(沉渊)` returns `Ambiguous([char_512_aprot, char_4025_aprot2])` — auto-attaching to one or both would either be arbitrary or silently broaden alias scope.
- **Risks table expanded:** updated grep-false-positives row to reflect class-aware floors; added rows for audit-cost ceiling and the entity-diff blind spot.

### 2026-05-08 — Design revisions after Codex reviews 07-consistency and 07-independent

**User intent:** Two more reviewer passes landed: `2026-05-08-codex-review-07-consistency.md` (4 findings; P1×2, P2, P3) and `2026-05-08-codex-review-07-independent.md` (3 findings; P1, P2×2, independent of prior context). User asked for a joint update round.

**Outcome:** Empirically reproduced each empirical claim — `司辰` is genuinely absent from `character_table.name`, `appellation`, `char_alias.txt`, and raw `character_table.json` (an embarrassing example I'd written); README / DESIGN.md inline drift confirmed by direct read; `Appearance` / `event_to_chars.json` shape inconsistency confirmed. All 7 findings applied:

- **P3 prompt rewritten and split** (R07c #1). Old single-prompt schema (`<遗漏>` + `<可疑描述>`) replaced with `P3a` (omission verification — Signal 1) and `P3b` (per-claim verdict — Signal 2). Each is per-item, matches the two-signal contract in DESIGN.md exactly. Output tags: P3a uses `判断 / 理由 / 原文证据`; P3b uses `判断 / 证据 / 说明`.
- **`Appearance` and `event_to_chars.json` flattened to one shape** (R07c #2). Both now use one row per `(char_id, stage_idx)`. No more aggregate-vs-flat mismatch. Inferred edges that span K stages produce K rows. Event-level rollup is the caller's responsibility via a `query.py` helper. Inline ASCII layout at DESIGN.md:105 updated to match.
- **Prune rule added to build contract** (R07c #3). `kb_build.py` deletes `events/<id>/` and `chars/<id>/` directories not referenced by the freshly computed manifest unless `--no-prune` is passed; same rule for `kb_summarize.py` against `kb_summaries/events/<id>.md`. Removed paths printed in build report. Closes the stale-file gap when upstream `ArknightsGameData` removes/renames an event.
- **README "01 & 02" status row updated to "01-07"** (R07c #4); inline ASCII shape (DESIGN.md:105) replaced as part of the flatten fix above.
- **Signal 1 broadened with NPC-shaped candidate extraction** (R07i #1). Code-only, regex `[一-鿿]{2,6}` with frequency ≥3 and stage-spread ≥2, blocklist-filtered, capped at top-20 per event. Closes the NPC-omission blind spot. False positives are surfaced to the LLM (P3a) for verdicts, never auto-attributed.
- **`match_class` threaded through `Appearance` and CLI** (R07i #2). Was internal-only despite being doc'd as a consumer-facing mitigation. Now part of `Appearance` and surfaced in `kb_query` JSON.
- **AGENTS_GUIDE.md ad-hoc Q/A example rewritten** (R07i #3). Replaced the impossible `司辰` example with two examples: (A) `陈` — real single-character operator that exercises `Resolved` + `match_class=canonical_short` caveat, (B) `特蕾西娅` — NPC that demonstrates the documented `Missing → grep` fallback path.

### 2026-05-08 — Phase 1 implementation kicked off

**User intent:** "Look at `arknights_lore_wiki_lib/docs/` and start the execution." User confirmed scope = Phase 1 only (paths + chunker + tests) and pytest (over unittest) as the test framework.

**Outcome:** Phase 1 landed:

- `libs/kb/__init__.py`, `libs/kb/paths.py`, `libs/kb/chunker.py` written.
- `libs/game_data.py` extended: `extract_data_from_character_table` now retains `appellation` (DESIGN.md "Aliases" pre-req).
- `tests/fixtures/mini_gamedata/` synthetic snapshot built (3 events / 6 stages / 3 chars covering all 4 source families + nameless `npc_*` skip case).
- `tests/test_paths.py` + `tests/test_chunker.py` — 56 tests, green.
- `pytest` added to `requirements.txt`; installed in `.venv`.
- M1–M5 measurement snippets re-run against full game data; numbers match DESIGN.md (461 events / 1937 stages, 372 storyset linked / 0 ambiguous, 444 named chars, etc.).
- **`source_family` classifier amended** to honor `entryType=MAINLINE` ahead of `storyTxt` prefix. Real data has `main_0` (the prologue, entryType=MAINLINE) whose first stage is `obt/guide/beg/0_welcome_to_guide` — a prefix-only rule misclassified it as `other`. Doc rule "mainline | storyTxt starts with `obt/main/` (also matches entryType=MAINLINE)" was implementable as either; now the classifier and DESIGN.md docstring both spell out entryType-first.

### 2026-05-08 — Phase 2 implementation landed (indexer + query)

**User intent:** Continue Phase 2 from the entry points the prior session left in `docs/README.md` and `docs/DESIGN.md#implementation-phases-proposed` — `libs/kb/indexer.py`, `libs/kb/query.py`, and the matching tests, with no LLM use.

**Outcome:** Phase 2 landed:

- `libs/kb/indexer.py` — pure-code builders for the six (or seven, with curated aliases) `data/kb/indexes/*.json` files. Implements: curated alias-file parser, ambiguous-canonical computation, `events_by_family` grouping, deterministic edges from `chars/<id>/storysets.json`, inferred-edge grep with the three-class match floor (canonical / canonical_short / curated; fuzzy reserved as a future class), per-(char, event) deterministic-subtraction rule, the highest-precision-class-wins aggregation per `(char, stage)` row, the flat one-row-per-(char, stage) `event_to_chars.json` shape, the resolver `alias_to_char_ids` index that attaches curated aliases to *all* owners when the canonical collides (so the resolver returns `Ambiguous` rather than picking arbitrarily). Atomic JSON writes via `os.replace`.
- `libs/kb/query.py` — pure-function retrieval API on top of a loaded `KB` dataclass: `load_kb`, family-aware `list_events` / `list_families`, `get_event` / `get_stage_text`, `list_chars` (nation filter), `resolve_operator_name` returning `Resolved | Ambiguous | Missing`, `get_char_section` (single section or `all`), `char_storysets`, `char_appearances` / `event_chars` / `stage_chars` returning `Appearance` (carrying `stage_idx`, `source`, `count`, `match_class`, `story_set_name` per the flat shape), `grep_text` literal-by-default with `regex=True` opt-in, `group_by_event` rollup helper, `get_event_summary` reading from optional `summaries_root`.
- `tests/test_indexer.py` (32 tests) + `tests/test_query.py` (33 tests) — full unit coverage of every public function, plus integration tests that build a real KB on disk via `chunker.write_event` / `write_char` against the mini fixture and exercise the indexer + query end-to-end. Hand-built KB helpers cover the ambiguous-canonical / collision cases that the mini fixture can't reproduce.
- Smoke-tested `parse_curated_alias_file` against the live `arknights_lore_wiki/data/char_alias.txt`: 265 canonicals (matches DESIGN.md "Aliases" measurement); spot-checks for `临光`, `凯尔希`, `暮落` parse correctly.
- Total test count: **127 passing.**

The KB is now retrieval-ready end-to-end *modulo* a `kb_build.py` script (Phase 3) that wires `chunker.write_event` + `write_char` + `indexer.build_all_indexes` into a single command. The `query.load_kb` / `query.*` API can already drive an agent against any KB built by hand; Phase 3 just packages the builder.


### 2026-05-08 — Phase 3 implementation landed (build + query CLIs)

**User intent:** After Phase 2 indexer + query went green, ship Phase 3 alone (no Phase 4 bundling) — the design called this out as the natural validation checkpoint where assumptions either survive contact with the full corpus or get revised before the LLM-using phases pile on. Branch was renamed `kb-phase-1` → `kb-build` to match scope.

**Outcome:** Phase 3 landed:

- `scripts/kb_build.py` — end-to-end deterministic build. Reads `keys.json` (fallback flags `--game-data-path`, `--wiki-path`, `--curated-aliases`, `--kb-root`). Snapshots `data_version.txt` and a 12-char SHA of `clean_script`'s source into `data/kb/manifest.json` so a future build can detect parser-driven cache staleness. Pre-computes ambiguous canonicals once and threads them into both `chunker.write_char` and the indexer so curated-alias attachment is consistent across both passes. Skips the 5 nameless `npc_*` records and lists them in the manifest. Prunes any leftover `events/<id>/` or `chars/<id>/` directory not in the new build (default on; `--no-prune` skips). Emits a printed report + a complete `manifest.json`.
- `scripts/kb_query.py` — argparse subcommand surface mirroring DESIGN.md§"Retrieval pipeline": `event {list,get,chars,stage_chars,stage}`, `family list`, `char {resolve,get,appearances,storysets}`, `grep`, `summary event`. JSON output by default, `--text` flag returns raw chunk text on the three text-returning commands. `char resolve` exits 2 on `Missing` so an agent can branch on exit code alone. Dataclasses + `Resolution` types serialize via `dataclasses.asdict` so the `kind` discriminator is preserved end-to-end.
- Real-data validation: `kb_build` runs in 8.2s against the live corpus and produces exactly the M-series numbers from DESIGN.md — 461 events (`mainline`:18, `activity`:51, `mini_activity`:20, `operator_record`:372, `other`:0), 444 named chars + 5 nameless skipped, 372 deterministic edges across 323 chars, 360 chars with inferred edges, 9 ambiguous canonicals (the documented `暮落 / 郁金香 / Sharp / Stormeye / Pith / Touch / 预备干员-{术师,狙击,重装}` set), 265 curated alias canonicals loaded, 0 storyset warnings. `main_0` correctly carries `storyTxt_prefixes: ["obt/guide", "obt/main"]` (the multi-prefix case). The `圣巡` Sticker-text canary surfaces in `act46side` stage 4 — confirms the `clean_script` Sticker fix flows through to the KB. Idempotent rebuild leaves the file count unchanged. Prune contract verified by injecting fake `events/zzz_dead_event/` + `chars/char_dead_xyz/` and confirming both disappear after rebuild.
- Test suite still **132 passing** — Phase 3 didn't perturb the libs.


### 2026-05-08 — Phases 4 + 5 implementation landed (LLM dispatch + first summarizer)

**User intent:** Bundle Phase 4 (`libs/llm_clients.py` — three-backend dispatch) with Phase 5 (`libs/kb/summarize.py` + `scripts/kb_summarize.py`) on one branch (`kb-summarize`) so the substrate ships alongside its first real consumer. Phase 4 alone has nothing to exercise it; pairing with Phase 5 gives end-to-end validation in one PR.

**Outcome:** Phase 4 + Phase 5 landed:

- **`libs/llm_clients.py`** — `LLMClient` Protocol + three implementations behind a single `make_client(backend, **kwargs)` factory. `GeminiCLIClient` (default model `gemini-3-flash-preview`), `GeminiSDKClient` (`gemini-2.5-flash`), `ClaudeCLIClient` (`claude-haiku-4-5`, with `shutil.which` check at instantiation per DESIGN.md "raises a clear error if missing"). All three honor `RETRY_LIMIT` / `RETRY_SLEEP_TIME` and surface `LLMError` on persistent failure. Gemini CLI invocation uses `--approval-mode plan` (read-only — refuses to call any tool) instead of `-y` (YOLO auto-approve); strictly safer for batch text-summarization where no tool calls are intended. Claude CLI invocation uses `--print --system-prompt --output-format json --no-session-persistence`, sends the prompt via stdin, and checks `is_error` so zero-exit rate-limit / blocked / model-not-found responses still raise instead of being treated as content. It intentionally avoids `--bare` so subscription/OAuth keychain auth continues to work. Added `query_with_validated_tags(client, system, prompt, required_tags)` helper — same retry-once-with-reminder semantics as `bases.query_llm_validated`, but takes a client directly (used by summarize and reusable for future audit code).
- **`libs/bases.py` refactor** — `query_llm` and `query_llm_validated` keep their public signatures but now delegate through `make_client`. The two old per-backend helpers (`query_llm_cli`, `query_llm_gai`) and their duplicated retry logic are deleted; `subprocess` / `time` imports follow them out. Existing scripts (`get_story_wiki`, `get_char_wiki_v3`) call sites stay byte-identical. Added a `TypeError` on unexpected kwargs to catch typos that the old `**kwargs`-blind dispatch would have silently lost.
- **`libs/kb/summarize.py`** — pure module: `summarize_event` for one event, `summarize_all` for the corpus. Routes to single-pass vs multi-pass on the M5 thresholds (`total_length > 80,000` OR `stage_count > 10`). Multi-pass = per-stage `章节概要 + 本章人物` reduce, then a merge call asking for the four event-level tags. Validates with `validate_and_rebuild` after a one-shot retry-with-reminder. Source-hash gate (sha256 over sorted stage filenames + bytes) keeps re-runs free unless `force=True` or the .md is missing on disk. Manifest at `kb_summaries/manifest.json` records the per-event hash + backend + model + generated-at. Prune is part of the contract (matches Phase 3): orphan `kb_summaries/events/<id>.md` files are dropped unless `--no-prune`. Output is markdown with frontmatter (`event_id`, `event_name`, `source_family`, `source_hash`, `passes`, `total_length`, `stage_count`, `backend`, `model`) followed by canonicalized zh tags.
- **`scripts/kb_summarize.py`** — CLI wrapper. `--llm cli|gai|claude` (default reads `keys.json llm_backend`), `--model`, `--event <id>` (repeatable), `--force`, `--no-prune`, `--kb-root`, `--summaries-root`. Reads keys.json for backend-specific defaults (`claude_cli_path`, `claude_model`, `gemini_cli_path`, `genai_api_key`). Exits non-zero if any per-event errors occurred but keeps going through the batch.
- **Real-LLM smoke test** on `story_12fce_set_1` (3,156 chars, single-pass): Gemini CLI returned a complete `<一句话概要> + <核心剧情> + <关键人物> + <场景标签>` block on second try (first attempt was missing `场景标签` — the retry-with-reminder mechanism fired and recovered, exactly as designed). Re-run was a no-op via the source-hash cache. `kb_summaries/events/story_12fce_set_1.md` and `kb_summaries/manifest.json` committed alongside the code as the canonical first artifact.
- **Model-name drift fix:** `keys.json llm_model` and `bases.DEFAULT_CLI_MODEL` were both set to `gemini-3.1-flash`, which the live Gemini API now rejects with `ModelNotFoundError`. Updated to `gemini-3-flash-preview` per the user. Surfaces here because the smoke test would have hung indefinitely without it.
- **`docs/PROMPTS.md` already had the P1 prompts** (single-pass + stage-reduce + merge); summarize.py mirrors them as Python constants. PROMPTS.md remains the canonical reviewable source.
- **Tests:** 184 passing total (132 prior + 27 new in `test_llm_clients.py` + 25 new in `test_summarize.py`). Mock-based throughout — `subprocess.run` and `genai.Client.models.generate_content` are monkeypatched; a `FakeClient` records every `(system, prompt, model)` tuple so call-count and prompt-shape assertions are exact. No real LLM in CI.

### 2026-05-11 — `kb_summarize --estimate` (no-LLM cost dry-run)

**User intent:** "How many tokens are needed to finish the kb summary work?" — asked frequently, so the user wanted it as reusable code + documented in the skill rather than recomputed by hand each time.

**Outcome:** Added `summarize.estimate_remaining()` / `estimate_event_cost()` in `libs/kb/summarize.py` (with `EST_*` output-size guesses and an `EST_CHARS_PER_TOKEN` divisor as tunables) and a `--estimate` flag on `scripts/kb_summarize.py`. The flag skips client construction entirely (no keys.json needed) and prints events-to-run (single vs multi pass), already-done count, total LLM calls, and projected input/output/total chars≈tokens. Honors `--event` / `--force` / `--kb-root` / `--summaries-root`; selection mirrors the real run (no manifest entry or missing `.md` → counts). Documented under Step 2 of the `refresh-kb` skill. Baseline 149/461 → ~312 events, ~559 calls, ~4.2M tokens (~3.8M in / ~0.4M out); a full `--force` re-bake ≈ ~13.2M tokens.

### 2026-05-11 — Live progress output during `kb_summarize` runs

**User intent:** A real (non-`--estimate`) `kb_summarize` run was silent for the entire multi-hour bake. The user wanted it to stream estimated progress per event — "added into the existing kb summary, output during run".

**Outcome:** `summarize_all` now accepts an optional `progress` callback and emits a `ProgressEvent` per event (position, status, running token estimate vs. the run's projected total, elapsed, token-rate ETA). Selection/estimate math is shared with `--estimate` via a new `_classify_run` helper (`estimate_remaining` was refactored onto it). `scripts/kb_summarize.py` passes a printer: `[i/N] <event_id>  +single|+multi  done X/Y ev  ~tok/tok  elapsed  ETA ~…` for writes, `· cached` for hash-skip hits, `✗ TERMINAL …` then stop on a terminal LLM error. The redundant per-event `+ <eid>` enumeration in the final report was dropped (progress already streamed it). `progress` defaults to `None`, so library callers / tests are unaffected (31/31 `test_summarize.py` still pass). README §4 and the `refresh-kb` skill Step 4 updated.

### 2026-05-11 — Lenient tag-format repair before re-asking the LLM

**User intent:** A flash kb_summarize run threw ~150 "missing tags; retrying" lines and 108 hard `LLMError: missing required tags after retry` failures — almost all on the *trailing* tag (`场景标签` for events, `本章人物` for stage-reduce). The user (who used to hand-fix these) asked whether JSON output / a heuristic repair / an LLM reformat call made more sense than blindly retrying. Decision: do the cheap, zero-token heuristic repair first; keep JSON-mode as a separate future call (it only really pays off with schema-constrained decoding, i.e. moving the KB summarizer off the gemini CLI to the `gai` SDK).

**Outcome:**
- `libs/bases.py`: added `_find_tag_content_lenient(text, tag)` + `repair_tag_format(text, required_tags) -> (text_or_rebuilt, still_missing)`. Salvages the common near-misses — an *unclosed* trailing tag (body runs to the next tag-ish line / EOF), full-width brackets `【】《》〈〉`, whitespace inside the delimiter `< 标签 >`, and markdown/bold/colon "label" lines (`### 场景标签`, `**场景标签**`, `场景标签：…`) — into canonical `<标签>…</标签>` (exactly what `validate_and_rebuild` would emit, so it's safe downstream). Strategy 3 (label form) requires real decoration (a markdown prefix or a trailing colon) so a prose line that merely starts with the tag word isn't mistaken for the body. If everything's already clean it returns the input untouched.
- `libs/llm_clients.py`: `query_with_validated_tags` now runs `repair_tag_format` first and only re-asks if a tag is *still* unrecoverable; on both the retry and the final `LLMError` it logs the offending output's head (`raw head: …`) since the raw response was previously discarded — these failures were undebuggable after the fact. `bases.query_llm_validated` delegates here, so the story/char generators get the same benefit for free.
- Tests: +9 in `test_llm_clients.py` covering each salvage shape, the no-decoration-prose negative case, the still-missing case, and that a salvageable response costs no second call. Suite 212 passing (was 203).
- Not done (deliberate): JSON/`response_schema` output, and re-asking the LLM to *reformat* (a cheaper reformat-only retry could be layered in later as the fallback before raising). Logging now means the next real run will tell us which residual shapes, if any, still slip through.

### 2026-05-11 — Archive every raw LLM response (kb_summarize)

**User intent:** "Each LLM output is expensive and might have value later" — keep the raw responses (including the malformed ones validation throws away, and the pre-canonicalization form of the good ones) in an archive that can be mined or purged later, rather than discarding them.

**Outcome:**
- `libs/bases.py`: module-level archive sink — `set_llm_archive_dir(path)` + `archive_llm_output(label, content, *, kind)`. Writes `<dir>/<YYYY-MM-DD>/<label>__<kind>__<HHMMSS_micro>.txt`; best-effort (never raises — a failed archive must not fail a summary); **off by default** so library callers and tests don't litter the working tree. (Also factored a tiny `_tag_block(tag, content)` shared by `validate_and_rebuild` and `repair_tag_format`, and `_sub_label` in `summarize.py` for the per-substep labels — `/simplify` pass.)
- `libs/llm_clients.py`: `query_with_validated_tags` gained an optional `archive_label`; when set (and archiving enabled) it stashes every raw response — `__try1` and, if it re-asks, `__try2` — so both the good output and any malformed one are kept.
- `libs/kb/summarize.py`: `_summarize_single_pass` / `_summarize_multi_pass` thread `archive_label`; `summarize_event` passes `event_id` (multi-pass stages get `<event_id>__stageNN`, the merge `<event_id>__merge`). So a full bake produces a per-call paper trail under one date dir.
- `scripts/kb_summarize.py`: resolves the archive dir (`--archive-dir` > keys.json `llm_archive_path` > default `./llm_archive`), `--no-archive` to disable, calls `bases.set_llm_archive_dir(...)` before the run, prints `raw-output archive: <dir|off>`. The `--estimate` path returns before any of this, untouched.
- `.gitignore`: added `llm_archive/` — these are large raw dumps; the validated summaries already live in git.
- Tests: +5 in `test_llm_clients.py` (disabled-by-default, writes-when-enabled, never-raises-on-bad-dir, archives-raw-output, archives-both-attempts-on-retry). Suite 217 passing (was 212). `test_summarize.py` unaffected — archiving stays off unless the global is set, which only `scripts/kb_summarize.py` does.
- Retention is manual: `rm -rf llm_archive/<date>` to purge. No auto-rotation (deliberate — the user wants to decide).

### 2026-05-12 — Completed the kb_summaries full bake (149 → 461)

**User intent:** "Continue the work, first with pro model" — finish the `kb_summarize` bake of the remaining ~312 events using the post-refactor pipeline (heuristic tag repair + raw-output archive from the 2026-05-11 work), starting with the `gemini-3-pro-preview` model.

**Outcome:** All 461 events now have summaries in `kb_summaries/events/`. The `gemini-3-pro-preview` run exhausted its daily quota after only **9 events** (`TerminalQuotaError`, ~23h reset) — so `gemini-3-pro-preview` is not viable for full bakes; **`gemini-3-flash-preview` stays the practical default**. The flash fallback run wrote the remaining 303 events in ~1h28m (~3.3M tokens) with **zero errors** and exactly **one** heuristic-repair retry (`story_mostma_set_1` — model emitted the English-ized tag `<core剧情>`, which the bracket heuristic can't salvage; the reminder-retry recovered it) — versus the ~150-retry / 108-hard-failure storm before the 2026-05-11 repair work, so that fix is confirmed effective. Source-hash cache meant the 9 pro-baked events weren't re-billed. `kb_summaries/events/*.md` (313 files changed: 312 new + manifest) committed; `llm_archive/2026-05-11/` (582 raw responses, 2.3 MB) left for manual purge.

### 2026-05-12 — Lore search/validation accuracy plan (chapter-grounded)

**User intent:** After the assistant fabricated a relationship ("黍 is the cook 弟弟 年 mentioned" — contradicted 黍's own 性别/排行), the user asked for a comprehensive plan to raise search *and* validation accuracy, using that error + the "十二岁兽" enumeration + char-page verification as test cases. Explicit constraints: token cost is acknowledged but secondary to correctness; retrieval should reach raw chapter (`<章节>`) text, guided per-chapter rather than per-event-summary. Also requested a manual a/b/c audit of `arknights_lore_wiki/data/char_v3/char_2025_shu.txt` to surface gaps first.

**Outcome:** Wrote `docs/proposals/lore-accuracy-plan.md`. Manual audit of the 黍 page found: 3 unresolved `<相关角色>` (绩/颉/神农 — NPCs/mentioned-only) + a suspicious `禾生→char_4119_wanqin` resolution; an authoritative-looking but largely *uncited* Sui birth-order (重岳=1/望=2/绩=3/黍=6/年=9/夕=11, only 黍=6 grounded in 档案资料一); event-level cites ("出自《怀黍离》") where chapter-level is needed; no fact card anchoring basics; and the meta-point that the v3 generator *correctly* avoided the 黍/弟弟 trap — so the prompt is fine, nothing *verifies* it. Plan = 6 phases: (P-A) deterministic per-char fact cards + make distilled layers searchable (`kb_query card`, `grep --in`, `event stages`); (P-B) per-stage summaries `kb_summaries/stages/<event>/<NN>.md` via `kb_summarize --stages` (≈ one bake); (P-C) relation network — deterministic co-occurrence graph + LLM-extracted-then-code-verified `kb_relations/<char>.jsonl` (in git, hash-gated, evidence-bearing, **unresolved object → `object:null` placeholder, never a guessed edge**) + curated override file; (P-D) build the Phase-6 `kb_audit_wiki` with passes `tags/xref/entity_diff/card_check/relation_check` (code) + `claim_grounding` (LLM, per-claim 有依据/无依据/不确定 + mandatory verbatim cite from raw `<章节>` chunks, retrieval = the *specific* stage chunks not event summaries); (P-E) run `kb_audit_summaries` over the 461 baked summaries + `kb_audit_wiki` over char_v3 pages; (P-F) `AGENTS_GUIDE.md` guardrail rules (every assertion cites a raw line; summaries are leads not sources; no transitive inference; hinted-but-unnamed → say so) + `refresh-kb` update + new `audit-wiki-page` skill. Adapts the archived 5-pass spec (`docs/proposals/audit-lore-wiki-prior-spec.md`) and Phase 6 of `docs/DESIGN.md`. Awaiting go-ahead on which phase to implement first (P-A is the cheap unlock).

### 2026-05-12 — Phase P-A done: deterministic fact cards + searchable summaries + `event stages`

**User intent:** Implement P-A of `docs/proposals/lore-accuracy-plan.md` — the cheap deterministic unlock.

**Outcome:**
- `libs/kb/cards.py`: `parse_bracket_block` (parses `【字段】值` handbook blocks; a field with no inline value absorbs the following lines — handles `矿石病感染情况`) + `build_card(char_id, char, storysets_linked)` → a JSON card with `name/appellation/nationId` (from `character_table`), `basic_info`/`physical_exam` (parsed from `基础档案`/`综合体检测试`), `objective_record` (`客观履历` verbatim), `archive_sections`, `skin_names`, `module_names`, `storysets`, and a `sources` map tagging each field with its origin table. NPCs/extended chars with no handbook get a card with empty parsed blocks but intact table fields.
- `chunker.write_char` now also writes `chars/<char_id>/card.json` (uses the `linked` storysets it already computed). `paths.char_card_path` added. Card lives inside the per-char dir for layout consistency + free pruning; `data/kb/` stays gitignored (regenerable in ~9s by `kb_build`).
- `kb_query`: new `char card <char_id_or_name>`; new `event stages <event_id>` (per-chapter listing — idx/name/avgTag/length/file/story_txt, straight from the event manifest); `grep --in` gained a `summaries` scope (searches `kb_summaries/events/*.md` — high-signal, no raw-script noise; no-op when summaries_root unset). `query.Match` gained a `source` field (`stage`|`char_section`|`event_summary`).
- Docs: `README.md` §3 + `docs/AGENTS_GUIDE.md` CLI surface updated.
- Tests: `tests/test_cards.py` (8) + 5 new in `test_query.py` (card parsed-from-handbook, unknown→None, `event stages`, unknown event→None, `grep --in summaries` incl. no-summaries-root no-op). Suite 228 passing (was 217).
- Verified live: `char card char_2025_shu` → `性别: 女`, `排行` not in card (correctly — that fact lives in `档案资料一` prose, not 基础档案), full archive-section list, modules `黍证章 / "钦天司时"`, storyset `谓我何求→story_shu_set_1`. `event stages act23side` lists all 章节 with `avgTag`. `grep "岁兽" --in summaries` → 13 hits across event summaries.
- Branch `kb-fact-cards`. Next phases per the plan: P-B per-stage summaries, P-C relation network, P-D `kb_audit_wiki`.

### 2026-05-12 — Plan rev. 2: add WS-0 (tiered participant extraction) before the relation network

**User intent:** Asked whether the plan is "too light on the char to event/chapter mapping" — they'd previously had a heuristic for it — and asked for a more comprehensive design doc to review before proceeding past P-A.

**Outcome:** Revised `docs/proposals/lore-accuracy-plan.md` to rev. 2. The current char↔stage mapping was assessed as too coarse: `indexer.build_char_to_events_inferred` does `body.count(alias)` per stage with only a 13-word blocklist + a per-class length floor, ignores the speaker signal already materialized in the chunk text (`clean_script` rewrites `[name="X"]Y → X:Y`), is substring not token (`年`⊂`年龄`, `W`⊂`World`, `Pith/Touch/Sharp` are English words), and emits only `count`+`match_class` with no confidence tier — so everything built on it (relation co-occurrence, audit `entity_diff`, "who appears in X" Q&A) inherits that noise. Added **WS-0**: a `build_stage_participants` extractor that tiers each char as `speaker` (≥1 line-leading `名字：…` dialogue line — deterministic, the default meaning of "appears in") / `named` (multi-char canonical in narration, or count≥N; ASCII/single-zh aliases require a word boundary) / `mentioned` (lone short/fuzzy hit, kept as recall floor but tier-marked), plus a third orthogonal deterministic edge layer `char_to_events_summary` derived from the `<关键人物>` tag of `kb_summaries` (resolved through the alias index; hash-gated free, no new LLM calls). `event_chars`/`stage_chars`/`char_appearances` gain `tier`; `kb_query` gets `--min-tier`. Added test case **TC-4** ("trustworthy who's-in-this-chapter"). Re-slotted phasing: P-A shipped → **P-B = WS-0** (deterministic, ~free, unblocks the rest) → P-C per-stage summaries → P-D relation network (co-occurrence now built *from* WS-0 edges; relation extraction sourced from chapters where WS-0 puts the char at speaker/named tier) → P-E `kb_audit_wiki` (`entity_diff` thresholds on WS-0 tiers) → P-F audit existing artifacts → P-G guardrails+skills. Ordering note in the doc: P-B before P-C recommended (WS-0 is free and everything downstream is only as good as the edges underneath), but they swap cleanly if the chapter-summary layer is wanted first. Awaiting review/go-ahead.

### 2026-05-12 — Plan rev. 3: close the entity-scope & citation-source contract gaps

**User intent:** A second review of `docs/proposals/lore-accuracy-plan.md` (rev. 2) surfaced six issues: (1) the relation graph can't satisfy TC-2 if `char resolve` is the object gate — named non-operators (绩/颉/神农) get collapsed to `object:null` or mis-resolved (禾生→char_4119_wanqin); (2) `claim_grounding` cites only event `<章节>` text but 黍's 排行 etc. live in handbook archive — valid dossier claims would be marked unsupported; (3) `tier="storyset"` is outside the `--min-tier {speaker,named,mentioned}` enum; (4) event-level `<关键人物>` edges can't carry an honest `stage_idx` until P-C; (5) Python `\b` doesn't do real word-boundary matching for single-CJK / ASCII-in-CJK names (rejects `年走进门`, not just `今年`); (6) the doc said ~3500 stages (actually 1937) and had a swapped phase reference. User: "sure" — fold all six into a rev. 3.

**Outcome:** Revised the plan to rev. 3. Changes: (a) first-class **named non-operator entities** — a relation/citation ref is `{name, entity_type, char_id|null}`; `char_id:null`+`name` set = a real node (lands in a deterministic `data/kb/entities.jsonl`, curated via `entities_curated.jsonl`); `name:null`+`hint` reserved for the genuinely unnamed (TC-1); "did `char resolve` miss?" is no longer the gate, "is there a name?" is. (b) fixed **citation-source taxonomy** — `event:<id>:stage:<NN>:line`, `char:<id>:archive:<section>`, `char:<id>:voice:<key>`, `card:<id>:<field>`; `claim_grounding`'s context bundle now includes the char's own card + handbook + the WS-0-tier-selected event chapters; a handbook-grounded claim is *supported*. (c) WS-0 edges carry separate `source ∈ {storyset,participant,summary}` and `tier ∈ {speaker,named,mentioned}` fields; `source=storyset` always passes `--min-tier`. (d) `summary`-source edges are event-scoped in P-B (surfaced in `event_chars`, not `stage_chars`); P-C upgrades to stage granularity. (e) replaced the naive `\b` shorthand with real per-script boundary logic (ASCII: neighbours not `[A-Za-z0-9]`; single-CJK: don't trust narration grep alone — require `speaker`/`count≥2`/`summary` to promote above `mentioned`) + two-way tests. (f) corrected to 1937 stages; fixed the P-B "unblocks P-E entity_diff + P-D co-occurrence" reference; P-D now ships the entity layer before the relation bake. Phasing order unchanged (P-A shipped → P-B WS-0 → P-C stage summaries → P-D entity layer + relations → P-E kb_audit_wiki → P-F audit existing → P-G guardrails). Awaiting go-ahead on P-B.

### 2026-05-12 — P-B shipped: WS-0 tiered participant char↔stage edges

**User intent:** "start P-B" — implement WS-0 from `docs/proposals/lore-accuracy-plan.md` rev. 3: replace the flat substring-count "inferred edge" pass with a tiered participant extractor, add an event-scoped `summary` edge layer from `<关键人物>`, and expose `--min-tier`.

**Outcome:** New `libs/kb/participants.py` (`build_stage_participants` + `build_char_to_events_summary` + `extract_speaker_names` + helpers). Char↔stage edges are now three layers — `deterministic` (storyset, unchanged, ground truth, always passes `--min-tier`), `participant` (per-stage, `tier ∈ {speaker,named,mentioned}` — speaker = had ≥1 `名字:台词` line; ASCII aliases word-boundary-matched so `W` ⊄ `World` but `W` ⊂ `W走`; single-zh-char aliases need ≥2 hits / a speaker line / a summary hit to clear `mentioned`; deterministic subtraction tightened to per-`(char,event,stage)`), and `summary` (event-scoped, `stage_idx:null`, from baked `kb_summaries/events/*.md` `<关键人物>` resolved via the alias index, hash-gated free, unresolved names reported in the build manifest). Index files: `char_to_events_inferred.json` → `char_to_events_participant.json`, plus new `char_to_events_summary.json`; `event_to_chars.json` merges all three. `query.Appearance` gains `tier`/`spoke_lines`/`mention_count`/`matched_aliases`, `stage_idx` becomes `int|None`, `count`/`match_class` removed from the public type. `kb_query event chars|stage_chars|char appearances` gain `--source {deterministic,participant,summary,all}` (default `all`) and `--min-tier {speaker,named,mentioned}` (default `named`); the old `--source both` is gone. `kb_build` reads `kb_summaries/` (default `./kb_summaries`, `--summaries-root` / non-dir to skip). Live build: 461 events / 444 chars, 372 deterministic + 21,773 participant + 1,487 summary edges, ~10s; 1,991 unresolved `<关键人物>` surface names across 433 events logged for later curation (the named-entity layer is plan phase P-D). Tests: new `tests/test_participants.py` (28 cases incl. the TC-4 regression through `query.event_chars`, ASCII boundary both directions, single-CJK noise, per-stage subtraction, summary parsing/drop/event-scoping); `tests/test_indexer.py` + `tests/test_query.py` updated; full suite 248 passing. Docs updated (README, AGENTS_GUIDE, DESIGN, CLAUDE.md). Branch `kb-participants` (stacked on `kb-fact-cards`). Note: `禾生 → char_4119_wanqin` (a known `char_alias.txt` mis-resolution) now also flows into the `summary` edge layer — out of scope for P-B; the rev-3 plan tracks the fix under the named-entity layer.

### 2026-05-12 — P-C (code): per-stage summaries (`kb_summarize --stages`)

**User intent:** "let's do P-C" — start plan phase P-C from `docs/proposals/lore-accuracy-plan.md`, motivated by wanting to "best utilize the token daily quota" (the per-stage bake is the long pole). Follow-up: "let's use the gemini cli with pro model first" (`--llm cli --model gemini-3.1-pro-preview`).

**Outcome:** `kb_summarize --stages` bakes one navigation summary per `<章节>` → `kb_summaries/stages/<event_id>/<NN>.md` (frontmatter: event_id/event_name/source_family/stage_idx/stage_name/avg_tag/source_hash/generated_at/total_length/backend/model; body: the same 4 tags as an event summary — `<一句话概要>`(≤30字) `<核心剧情>`(≤200字) `<关键人物>` `<场景标签>`). Always single-pass — no stage chunk approaches the multi-pass threshold (max stage ≈ 21K chars). New prompt `USER_PROMPT_STAGE_SUMMARY` (distinct from the P1 multi-pass per-stage *reduce* prompt, which is an internal intermediate). `libs/kb/summarize.py` refactored: the event and stage bakes now share a generic `_run_batch` (hash-gated, persists the manifest after every write so a kill/quota wall mid-bake never loses paid-for work, bails the whole batch on a terminal LLM error) — `summarize_all` / `summarize_all_stages` / `estimate_remaining` / `estimate_remaining_stages` all build `_BatchUnit`s and hand them to it; `summarize_event` (the public single-event API) keeps its `SummaryResult` contract, delegating the LLM+format core to a shared `_produce_event_summary`. `kb_summaries/manifest.json` gains a `stages` section (keyed `"<event_id>/<NN>"`), disjoint from `events`. `paths.stage_summary_path` etc. added. CLI: `kb_summarize --stages [--event …] [--force] [--estimate] [--no-prune]`; `--stages --estimate` reports the dry-run (`~1937 stages, ~13M tokens` corpus-wide); the end-of-run summary now flags `terminal_error` ("re-run to resume"). Pruning extended to `kb_summaries/stages/` (drops stale `<NN>.md` and now-empty event dirs on a full, completed run). Tests: ~18 new in `tests/test_summarize.py` (one-per-stage write + frontmatter + chapter-text-in-prompt, hash-skip noop, `--event` filter, `--force`, prune / no-prune / only-skips-prune, missing-stage-file continues, empty-KB raises, `estimate_remaining_stages` counts-all-then-none, `prune_stale_stage_summaries` direct, event+stage bakes share one manifest); full suite 263 passing. Smoke-tested one stage live (`story_12fce_set_1/00` via `gemini-3.1-pro-preview` — clean output, correct frontmatter/manifest; artifact reverted so the code commit stays clean). **Not done in this commit (tracked follow-up):** the `<关键人物>`→stage-granular `summary`-edge upgrade in `indexer`/`kb_build` — it does nothing until the bake exists, so it can land while/after the user runs the ~1937-stage bake. Docs updated (README §4, AGENTS_GUIDE, DESIGN, PROMPTS §P1b, plan-doc status + P-C row, CLAUDE.md). Branch `kb-stage-summaries` (stacked on `kb-participants`).

### 2026-05-13 — P-D (entity layer, first slice): `entities.jsonl` + `kb_query entity …`

**User intent:** P-C bake was in flight (the user's to run, multi-hour). User asked whether the next plan phase could run in parallel; the answer was yes — P-D's entity layer is deterministic, reads only `character_table` / `char_alias.txt` / baked `kb_summaries/`, and never writes to anything the bake touches. Confirmed: "yes" → "just work on main" → after the surfaced constraint that switching off `kb-stage-summaries` while the bake holds an unstaged tracked file (`kb_summaries/manifest.json`) would corrupt resume state, settled on developing P-D as commits on the in-flight branch and sorting out branch structure once the bake finishes.

**Outcome:** First slice of P-D shipped — the entity layer per `docs/proposals/lore-accuracy-plan.md` rev. 3. No relation bake yet (deferred; that one is LLM-bound and would compete with P-C for tokens).

- `libs/kb/entities.py`: `build_entities(...)` produces three row classes — **operator** (`id = char_id`, aliases = `name`+`appellation`+curated `char_alias.txt` when unambiguous), **curated NPC** (from optional `<wiki>/data/entities_curated.jsonl`; `id = "ent_<6hex>"` from sha256 of utf-8 name bytes), **auto-seeded unknown** (from `<关键人物>` surface names in baked event + stage summaries that no operator alias resolves). Precedence: a curated entry whose `name` is an operator alias is dropped with a warning (operator wins); an auto-seed whose `name` is already curated is dropped silently. `name:null` hints stay outside (the parser requires `name`, so TC-1's "hint, never a guessed id" is structurally honest). Output: `data/kb/entities.jsonl` (gitignored, regenerable; one JSON object per line, operators sorted first).
- `paths.entities_jsonl_path` + `paths.curated_entities_path(wiki_path)` helpers added; build wires into the existing `indexer.build_all_indexes` (local-imports `entities` to avoid the same circular-import shape `participants` has).
- `scripts/kb_build`: `--curated-entities` flag with the same `--wiki-path`-lookup pattern as `--curated-aliases`; entity counts + curated errors/warnings surface in the manifest and the printed report.
- `libs/kb/query` + `scripts/kb_query`: KB struct gains `entities`/`entities_by_id`/`entity_alias_to_ids`. New `entity` subcommand group: `resolve <name>` (tagged sum `ResolvedEntity | AmbiguousEntity | MissingEntity` mirroring `resolve_operator_name`'s shape; returns rc 0 on resolve, 1 on missing), `list [--type ...]`, `get <entity_id>`. The operator-only `char resolve` stays; the new `entity resolve` is the broader gate the rev-3 plan calls for.
- `tests/test_entities.py`: 20 cases — `synthetic_entity_id` determinism, operator-row aliases incl. `name==appellation` dedup + curated-skip-when-ambiguous, curated-parser happy/missing-file/operator-type-rejected/bad-line accumulation, curated-build drops operator collisions, auto-seed skips curated names, top-level integration with all three sources + the `阿米娅`-collision warning case + operator-first sort, round-trip I/O, alias-index multi-target rows. Full suite 288 passing (268 prior + 20). Smoke-tested CLI on the mini fixture (`entity list / resolve / get`) — output shape matches the schema in the entities.py docstring.
- The unresolved `<关键人物>` names already surfaced by `kb_build`'s manifest (1,991 names across 433 events from the prior P-B run, plus whatever the in-flight stage bake adds) become auto-seeded `entity_type="unknown"` rows the next time `kb_build` runs against summaries — the curator's punch list for promoting 绩/颉/神农/罗德岛/叙拉古/上尉/etc. into typed entries via `entities_curated.jsonl`.
- Deferred to a P-D follow-up (after the bake finishes): the co-occurrence graph built from WS-0's tiered edges (deterministic, ~free) + the relation-extraction LLM bake (`kb_relations/<char_id>.jsonl`, hash-gated, evidence-bearing). Co-occurrence is the natural next deterministic step once P-C is done.
- Branch: commits live on `kb-stage-summaries` for now (the only safe place to write while the bake holds `kb_summaries/manifest.json`); to be cherry-picked onto a clean `kb-entities`-off-`main` once the bake completes.

### 2026-05-12 — P-C follow-up: stage-granular `summary`-source char↔stage edges

**User intent:** "you should continue" — finish the P-C follow-up flagged in the prior entry (wiring `indexer`/`kb_build` to read `kb_summaries/stages/`).

**Outcome:** `participants.build_char_to_events_summary` now reads `kb_summaries/stages/<event_id>/<NN>.md` in addition to `events/<id>.md`: each `<关键人物>` of a baked stage summary becomes a *stage-scoped* `summary` row (`stage_idx` = the chapter index), suppressed when a deterministic edge already links `(char, event, stage)`; the event-scoped row from `events/<id>.md` is kept only for `(char, event)` pairs *not* covered by any stage-scoped row (the stage breakdown subsumes it — partial bakes stay honest). `unresolved` names are merged (sorted/deduped) across both layers. No `indexer`/`kb_build`/`query` API change — the existing call picks up `kb_summaries/stages/` automatically; a stage-scoped `summary` row flows into `stage_chars`/`char_appearances` (it has a real `stage_idx`). +5 tests in `tests/test_participants.py` (stage-scoped rows, stage subsumes event-scoped, per-`(char,event,stage)` deterministic subtraction, unresolved merged across layers, inversion includes stage rows); full suite 268 passing. Live-verified: baked `story_12fce_set_1/00`, ran `kb_build` → `summary_edge_count` 1487→1488, `char_503_rang` picks up `{event_id: story_12fce_set_1, stage_idx: 0, source: summary}` (was event-scoped before); artifacts reverted. Docs updated (AGENTS_GUIDE, DESIGN, plan-doc P-C row + status). Branch `kb-stage-summaries` (2nd commit).

### 2026-05-14 — Schema illustration page (`docs/schema/`)

**User intent:** Visualize the KB's concept-level shape (character / NPC / story / event / edge layers / queries) — not the actual data graph — as an interactive HTML page committable to GitHub and viewable on the web, so weaknesses in the agent's lookup/exploration paths surface visually rather than only as buried text in DESIGN.md. Should be maintainable as the KB evolves.

**Outcome:** New `docs/schema/` folder: `index.html` (Cytoscape.js + fcose + dagre loaded from jsDelivr, CDN-pinned versions), `schema.json` (53 nodes, 80 edges, 9 layers — the single source of truth), `README.md` (Pages setup + maintenance rule). Layers cover gamedata · curation · raw KB · derived indexes · LLM-baked summaries · logical concepts · query/audit surface · published wiki · agent. Each node carries `summary` / `details` / `path` / `module`; nodes with a `weakness` field surface a warning callout in the side panel and can be highlighted via a top-bar toggle. `built: false` / `"wip"` flags work-in-progress nodes (`der-relations`, `der-cooccur`) so the picture reflects the current branch state, not just the design. Wired from `docs/README.md` (Files table) and root `CLAUDE.md` (KB section). Pages serves from `/docs` on `main` once enabled in repo settings → live at `https://<owner>.github.io/<repo>/schema/`; htmlpreview.github.io works as a no-setup fallback. Direct view on github.com does not work (GitHub's file viewer doesn't execute JS). The schema is hand-curated, not code-generated — maintenance rule documented in `docs/schema/README.md` lists when to edit `schema.json` (new module / new index / new query subcommand / changed edge derivation rule / promoting WIP→merged). Authored in worktree `worktree-kb-schema-viz` off main; current `kb-relations` branch's WIP modules (`cooccurrence.py`, `relations.py`) are represented as `built: "wip"` so the picture stays accurate until those land.
